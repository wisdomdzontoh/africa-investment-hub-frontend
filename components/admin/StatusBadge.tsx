// Thin alias kept for import stability; the DS status pill is the source of
// truth (red = approved/trust, grey = pending, solid ink = rejected/suspended).
export { StatusPill as StatusBadge } from "@/components/portal/StatusPill";
